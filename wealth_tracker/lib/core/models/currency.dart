/// Currencies selectable when entering an asset value or a ledger amount.
/// Kept short and explicit rather than the full ISO-4217 list, per the
/// household's actual needs.
const supportedCurrencies = ['EGP', 'USD', 'EUR', 'SAR', 'AED', 'TRY'];

const defaultCurrency = 'EGP';
